# BLE/WiFi coexistence. See coex.overlay for the pin mapping and why this is
# opt-in. The nRF70 arbitrates with the SoC's radio over a 4-wire PTA link;
# without it the two radios share 2.4 GHz with nothing mediating.
CONFIG_NRF70_SR_COEX=y
CONFIG_NRF70_SR_COEX_RF_SWITCH=n
