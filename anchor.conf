# Anchor role overlay:  west build -- -DEXTRA_CONF_FILE=anchor.conf
CONFIG_IMPULSE_ROLE_ANCHOR=y

# The GAP name is per-role; both were advertising as "Impulse Watch", which is
# confusing in a scan list showing several devices.
CONFIG_BT_DEVICE_NAME="Impulse Anchor"
